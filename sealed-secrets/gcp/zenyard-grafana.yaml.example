# PLACEHOLDER — not applied. The real `zenyard-grafana.yaml` is generated by
# `make gcp-seal-secrets` against the remote cluster and committed alongside this file.
# Consumed by: kube-prometheus-stack Grafana (admin.existingSecret: zenyard-grafana).
# Keys: admin-user, admin-password.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: zenyard-grafana
  namespace: zenyard
spec:
  encryptedData:
    admin-user: AgB...<encrypted>...
    admin-password: AgB...<encrypted>...
  template:
    metadata:
      name: zenyard-grafana
      namespace: zenyard
    type: Opaque
